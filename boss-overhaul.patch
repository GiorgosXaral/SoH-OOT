diff --git a/soh/include/boss_overhaul.h b/soh/include/boss_overhaul.h
new file mode 100644
index 0000000..f9c9204
--- /dev/null
+++ b/soh/include/boss_overhaul.h
@@ -0,0 +1,28 @@
+#ifndef BOSS_OVERHAUL_H
+#define BOSS_OVERHAUL_H
+
+/**
+ * Boss Overhaul
+ *
+ * Shared helpers for the "Boss Overhaul" enhancement (Enhancements > Difficulty > Enemies).
+ *
+ *  - Double Boss HP:     every main boss needs twice the damage to be defeated.
+ *  - Extra Boss Attacks: every main boss gets additional attacks, and fights the player more aggressively as its
+ *                        health runs low ("rage" phase). Only vanilla actors, effects, sounds and models are used.
+ *
+ * Both options are enabled by default and can be toggled independently from the menu.
+ */
+
+#include <libultraship/bridge/consolevariablebridge.h>
+#include "soh/cvar_prefixes.h"
+
+#define BO_HP_ENABLED() (CVarGetInteger(CVAR_ENHANCEMENT("BossOverhaul.DoubleHP"), 1) != 0)
+#define BO_ATTACKS_ENABLED() (CVarGetInteger(CVAR_ENHANCEMENT("BossOverhaul.ExtraAttacks"), 1) != 0)
+
+// Scales a boss health constant (the vanilla value is kept when Double Boss HP is disabled).
+#define BO_HP(vanilla) (BO_HP_ENABLED() ? ((vanilla) * 2) : (vanilla))
+
+// Convenience for "rage" checks: true when `cur` is at or below half of the boss' (possibly doubled) max health.
+#define BO_BELOW_HALF(cur, vanillaMax) ((cur) <= (BO_HP(vanillaMax) / 2))
+
+#endif
diff --git a/soh/soh/SohGui/SohMenuEnhancements.cpp b/soh/soh/SohGui/SohMenuEnhancements.cpp
index 4cf7305..56562ab 100644
--- a/soh/soh/SohGui/SohMenuEnhancements.cpp
+++ b/soh/soh/SohGui/SohMenuEnhancements.cpp
@@ -1347,6 +1347,17 @@ void SohMenu::AddMenuEnhancements() {
 
     path.column = SECTION_COLUMN_3;
     AddWidget(path, "Enemies", WIDGET_SEPARATOR_TEXT);
+    AddWidget(path, "Boss Overhaul: Double HP", WIDGET_CVAR_CHECKBOX)
+        .CVar(CVAR_ENHANCEMENT("BossOverhaul.DoubleHP"))
+        .Options(CheckboxOptions().DefaultValue(true).Tooltip(
+            "All main bosses (Gohma through Ganon) have twice as much health.\n\n"
+            "Takes effect the next time a boss fight starts."));
+    AddWidget(path, "Boss Overhaul: Extra Attacks", WIDGET_CVAR_CHECKBOX)
+        .CVar(CVAR_ENHANCEMENT("BossOverhaul.ExtraAttacks"))
+        .Options(CheckboxOptions().DefaultValue(true).Tooltip(
+            "All main bosses (Gohma through Ganon) gain additional attacks, and become more aggressive once "
+            "they are badly hurt.\n\n"
+            "Takes effect the next time a boss fight starts."));
     AddWidget(path, "Hyper Bosses", WIDGET_CVAR_CHECKBOX)
         .CVar(CVAR_ENHANCEMENT("HyperBosses"))
         .Callback([](WidgetInfo& info) { UpdateHyperBossesState(); })
diff --git a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
index 16391b3..a391177 100644
--- a/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
+++ b/soh/src/overlays/actors/ovl_Boss_Dodongo/z_boss_dodongo.c
@@ -11,6 +11,9 @@
 #include <stdlib.h> // malloc
 #include <string.h> // memcpy
 
+#include "boss_overhaul.h"
+#include "overlays/actors/ovl_En_Bdfire/z_en_bdfire.h"
+
 #define FLAGS                                                                                 \
     (ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_HOSTILE | ACTOR_FLAG_UPDATE_CULLING_DISABLED | \
      ACTOR_FLAG_DRAW_CULLING_DISABLED)
@@ -329,7 +332,7 @@ void BossDodongo_Init(Actor* thisx, PlayState* play) {
     Animation_PlayLoop(&this->skelAnime, &object_kingdodongo_Anim_00F0D8);
     this->unk_1F8 = 1.0f;
     BossDodongo_SetupIntroCutscene(this, play);
-    this->health = 12;
+    this->health = BO_HP(12);
     this->colorFilterMin = 995.0f;
     this->actor.colChkInfo.mass = MASS_IMMOVABLE;
     this->colorFilterMax = 1000.0f;
@@ -630,6 +633,43 @@ void BossDodongo_IntroCutscene(BossDodongo* this, PlayState* play) {
     }
 }
 
+// Boss Overhaul: King Dodongo is enraged once he is down to half of his (possibly doubled) health.
+static s32 BossDodongo_IsEnraged(BossDodongo* this) {
+    return BO_ATTACKS_ENABLED() && this->health <= BO_HP(12) / 2;
+}
+
+// Boss Overhaul: fire breath fired at an angle relative to Dodongo's facing direction.
+static void BossDodongo_SpawnFireAngled(BossDodongo* this, PlayState* play, s16 params, s16 yawOffset) {
+    Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_EN_BDFIRE, this->vec.x, this->vec.y - 20.0f,
+                       this->vec.z, 0, this->actor.shape.rot.y + yawOffset, 0, params);
+}
+
+// Boss Overhaul: a stationary patch of flames left behind while Dodongo rolls.
+static void BossDodongo_SpawnFirePatch(BossDodongo* this, PlayState* play, s16 lifetime) {
+    Actor* fire = Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_EN_BDFIRE, this->actor.world.pos.x,
+                                     this->actor.world.pos.y + 10.0f, this->actor.world.pos.z, 0,
+                                     this->actor.shape.rot.y, 0, 4);
+
+    if (fire != NULL) {
+        fire->speedXZ = 0.0f;
+        ((EnBdfire*)fire)->unk_154 = lifetime;
+    }
+}
+
+// Boss Overhaul: ring of flames that shoots outwards from Dodongo's body.
+static void BossDodongo_FireNova(BossDodongo* this, PlayState* play) {
+    s16 i;
+    s16 count = 8;
+
+    Audio_PlayActorSound2(&this->actor, NA_SE_EN_DODO_K_CRY);
+    func_80033E88(&this->actor, play, 4, 10);
+
+    for (i = 0; i < count; i++) {
+        Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_EN_BDFIRE, this->actor.world.pos.x,
+                           this->actor.world.pos.y + 20.0f, this->actor.world.pos.z, 0, i * (0x10000 / count), 0, 6);
+    }
+}
+
 void BossDodongo_SetupDamaged(BossDodongo* this) {
     if (this->actionFunc != BossDodongo_Damaged) {
         Animation_Change(&this->skelAnime, &object_kingdodongo_Anim_001074, 1.0f, 0.0f,
@@ -694,6 +734,9 @@ void BossDodongo_Damaged(BossDodongo* this, PlayState* play) {
     Math_SmoothStepToF(&this->unk_208, 0.05f, 1.0f, 0.005f, 0.0f);
 
     if (Animation_OnFrame(&this->skelAnime, Animation_GetLastFrame(&object_kingdodongo_Anim_001074))) {
+        if (BossDodongo_IsEnraged(this)) {
+            BossDodongo_FireNova(this, play);
+        }
         BossDodongo_SetupRoll(this);
     }
 }
@@ -751,7 +794,7 @@ void BossDodongo_LayDown(BossDodongo* this, PlayState* play) {
         Animation_Change(&this->skelAnime, &object_kingdodongo_Anim_0042A8, 1.0f, 0.0f,
                          Animation_GetLastFrame(&object_kingdodongo_Anim_0042A8), ANIMMODE_LOOP, -5.0f);
         this->actionFunc = BossDodongo_Vulnerable;
-        this->unk_1DA = 100;
+        this->unk_1DA = BossDodongo_IsEnraged(this) ? 75 : 100;
     }
 }
 
@@ -794,6 +837,11 @@ void BossDodongo_BlowFire(BossDodongo* this, PlayState* play) {
 
     if ((this->skelAnime.curFrame > 17.0f) && (this->skelAnime.curFrame < 35.0f)) {
         BossDodongo_SpawnFire(this, play, this->unk_1AE);
+        if (BO_ATTACKS_ENABLED() && (this->unk_1AE % 3) == 0) {
+            // Boss Overhaul: the fire breath fans out to both sides
+            BossDodongo_SpawnFireAngled(this, play, this->unk_1AE, 0x2400);
+            BossDodongo_SpawnFireAngled(this, play, this->unk_1AE, -0x2400);
+        }
         this->unk_1AE++;
         Math_SmoothStepToF(&this->unk_244, 0.0f, 1.0f, 8.0f, 0.0f);
     }
@@ -925,7 +973,7 @@ void BossDodongo_Roll(BossDodongo* this, PlayState* play) {
     }
 
     sp5C = &sCornerPositions[this->unk_1A0];
-    this->unk_1EC = 3.0f;
+    this->unk_1EC = BossDodongo_IsEnraged(this) ? 3.8f : 3.0f;
 
     if (this->unk_1DA == 0) {
         Math_SmoothStepToF(&this->unk_1E4, this->unk_1EC * 5.0f, 1.0f, this->unk_1EC * 0.25f, 0.0f);
@@ -944,6 +992,15 @@ void BossDodongo_Roll(BossDodongo* this, PlayState* play) {
             if (!(this->unk_19E & 1)) {
                 Actor_SpawnFloorDustRing(play, &this->actor, &this->actor.world.pos, 40.0f, 3, 8.0f, 500, 10, false);
             }
+
+            if (BO_ATTACKS_ENABLED()) {
+                // Boss Overhaul: Dodongo leaves burning patches on the floor while rolling
+                s32 enraged = BossDodongo_IsEnraged(this);
+
+                if ((this->unk_19E % (enraged ? 6 : 12)) == 0) {
+                    BossDodongo_SpawnFirePatch(this, play, enraged ? 70 : 45);
+                }
+            }
         }
     }
 
diff --git a/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.c b/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.c
index 4b050ab..2eda730 100644
--- a/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.c
+++ b/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.c
@@ -16,8 +16,9 @@
 #include "soh/frame_interpolation.h"
 #include "soh/Enhancements/game-interactor/GameInteractor.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
+#include "boss_overhaul.h"
 
-#define FLAGS                                                                                 \
+#define FLAGS                                                                               \
     (ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_HOSTILE | ACTOR_FLAG_UPDATE_CULLING_DISABLED | \
      ACTOR_FLAG_DRAW_CULLING_DISABLED)
 
@@ -216,7 +217,7 @@ void BossFd_Init(Actor* thisx, PlayState* play) {
         }
     }
 
-    this->actor.colChkInfo.health = 24;
+    this->actor.colChkInfo.health = BO_HP(24);
     this->skinSegments = 18;
     if (this->introState == BFD_CS_NONE) {
         this->actionFunc = BossFd_Wait;
@@ -251,6 +252,49 @@ s32 BossFd_IsFacingLink(BossFd* this) {
     return ABS((s16)(this->actor.yawTowardsPlayer - this->actor.world.rot.y)) < 0x2000;
 }
 
+/**
+ * Boss Overhaul: Volvagia's health expressed on the vanilla 24 point scale, so that the phase thresholds keep working
+ * when Double Boss HP is enabled (a doubled 48 / 47 both count as 24, 46 / 45 as 23, and so on).
+ */
+s16 BossFd_GetPhaseHealth(BossFd* this) {
+    s16 health = (s8)this->actor.colChkInfo.health;
+
+    if (BO_HP_ENABLED()) {
+        return (health + 1) / 2;
+    }
+    return health;
+}
+
+/**
+ * Boss Overhaul: Volvagia is enraged in the second half of the fight when extra attacks are enabled.
+ */
+s32 BossFd_IsEnraged(BossFd* this) {
+    return BO_ATTACKS_ENABLED() && (BossFd_GetPhaseHealth(this) <= 12);
+}
+
+/**
+ * Boss Overhaul: drops one of Volvagia's own ceiling rocks (ACTOR_EN_VB_BALL) from the ceiling onto (x, z).
+ */
+void BossFd_SpawnBonusRock(BossFd* this, PlayState* play, f32 x, f32 z) {
+    EnVbBall* rock = (EnVbBall*)Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_EN_VB_BALL, x, 1000.0f, z,
+                                                   0, 0, (s16)Rand_ZeroFloat(50.0f) + 130, 100);
+    s16 i;
+
+    if (rock != NULL) {
+        for (i = 0; i < 10; i++) {
+            Vec3f debrisVel = { 0.0f, 0.0f, 0.0f };
+            Vec3f debrisAccel = { 0.0f, -1.0f, 0.0f };
+            Vec3f debrisPos;
+
+            debrisPos.x = Rand_CenteredFloat(300.0f) + rock->actor.world.pos.x;
+            debrisPos.y = Rand_CenteredFloat(300.0f) + rock->actor.world.pos.y;
+            debrisPos.z = Rand_CenteredFloat(300.0f) + rock->actor.world.pos.z;
+
+            BossFd_SpawnDebris(this->effects, &debrisPos, &debrisVel, &debrisAccel, (s16)Rand_ZeroFloat(15.0f) + 20);
+        }
+    }
+}
+
 void BossFd_SetupFly(BossFd* this, PlayState* play) {
     Animation_PlayOnce(&this->skelAnimeHead, &gVolvagiaHeadEmergeAnim);
     Animation_PlayOnce(&this->skelAnimeRightArm, &gVolvagiaRightArmEmergeAnim);
@@ -600,7 +644,8 @@ void BossFd_Fly(BossFd* this, PlayState* play) {
                         this->work[BFD_FLY_COUNT]++;
                         if (this->work[BFD_FLY_COUNT] & 1) {
                             this->work[BFD_ACTION_STATE] = BOSSFD_FLY_CHASE;
-                            this->timers[0] = 300;
+                            // Boss Overhaul: longer chase
+                            this->timers[0] = BO_ATTACKS_ENABLED() ? 400 : 300;
                             this->fwork[BFD_TURN_RATE_MAX] = 900.0f;
                             this->fwork[BFD_TARGET_Y_OFFSET] = 300.0f;
                             this->work[BFD_UNK_234] = this->work[BFD_UNK_236] = 0;
@@ -660,7 +705,8 @@ void BossFd_Fly(BossFd* this, PlayState* play) {
             if (this->actor.world.pos.y > 700.0f) {
                 this->work[BFD_ACTION_STATE] = BOSSFD_DROP_ROCKS;
                 this->timers[0] = 25;
-                this->timers[2] = 150;
+                // Boss Overhaul: longer rock storm
+                this->timers[2] = BO_ATTACKS_ENABLED() ? 210 : 150;
                 this->work[BFD_CEILING_TARGET] = 0;
             }
             break;
@@ -697,7 +743,8 @@ void BossFd_Fly(BossFd* this, PlayState* play) {
             if (this->timers[2] == 0) {
                 this->work[BFD_ACTION_STATE] = BOSSFD_FLY_MAIN;
                 this->timers[0] = 0;
-                this->work[BFD_START_ATTACK] = false;
+                // Boss Overhaul: when enraged, the rock storm flows straight into a chase (rocks -> chase combo)
+                this->work[BFD_START_ATTACK] = BossFd_IsEnraged(this);
             }
             break;
         case BOSSFD_FLY_CHASE:
@@ -707,11 +754,20 @@ void BossFd_Fly(BossFd* this, PlayState* play) {
             this->targetPosition.y = player->actor.world.pos.y + temp_y + 30.0f;
             this->targetPosition.z = player->actor.world.pos.z;
             this->fwork[BFD_FLY_WOBBLE_AMP] = 0.0f;
-            if (((this->timers[0] % 64) == 0) && (this->timers[0] < 450)) {
+            if (((this->timers[0] % (BO_ATTACKS_ENABLED() ? (BossFd_IsEnraged(this) ? 32 : 48) : 64)) == 0) &&
+                (this->timers[0] < 450)) {
                 this->work[BFD_ROAR_TIMER] = 40;
                 if (BossFd_IsFacingLink(this)) {
                     this->fireBreathTimer = 20;
                 }
+                // Boss Overhaul: an enraged Volvagia also calls a rock down on top of Link with every roar
+                if (BossFd_IsEnraged(this) && (this->timers[0] != 0)) {
+                    BossFd_SpawnBonusRock(this, play, player->actor.world.pos.x, player->actor.world.pos.z);
+                }
+            }
+            if (BossFd_IsEnraged(this)) {
+                // Boss Overhaul: faster chase
+                this->fwork[BFD_FLY_SPEED] = 7.0f;
             }
             if ((this->work[BFD_DAMAGE_FLASH_TIMER] != 0) || (this->timers[0] == 0) ||
                 (player->actor.world.pos.y < 70.0f)) {
@@ -1299,8 +1355,8 @@ void BossFd_CollisionCheck(BossFd* this, PlayState* play) {
         if (hurtbox->toucher.dmgFlags & 0x1000) {
             this->actor.colChkInfo.health -= 2;
         }
-        if ((s8)this->actor.colChkInfo.health <= 2) {
-            this->actor.colChkInfo.health = 2;
+        if ((s8)this->actor.colChkInfo.health <= BO_HP(2)) {
+            this->actor.colChkInfo.health = BO_HP(2);
         }
         this->work[BFD_DAMAGE_FLASH_TIMER] = 10;
         this->work[BFD_INVINC_TIMER] = 20;
@@ -1368,7 +1424,8 @@ void BossFd_Update(Actor* thisx, PlayState* play) {
 
     if (this->work[BFD_ROCK_TIMER] != 0) {
         this->work[BFD_ROCK_TIMER]--;
-        if ((this->work[BFD_ROCK_TIMER] % 16) == 0) {
+        // Boss Overhaul: denser rock storm
+        if ((this->work[BFD_ROCK_TIMER] % (BO_ATTACKS_ENABLED() ? (BossFd_IsEnraged(this) ? 8 : 12) : 16)) == 0) {
             EnVbBall* bossFdRock = (EnVbBall*)Actor_SpawnAsChild(
                 &play->actorCtx, &this->actor, play, ACTOR_EN_VB_BALL, this->actor.world.pos.x, 1000.0f,
                 this->actor.world.pos.z, 0, 0, (s16)Rand_ZeroFloat(50.0f) + 130, 100);
diff --git a/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.h b/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.h
index 60d7ae1..8c30772 100644
--- a/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.h
+++ b/soh/src/overlays/actors/ovl_Boss_Fd/z_boss_fd.h
@@ -183,4 +183,9 @@ typedef struct BossFd {
     /* 0x1970 */ BossFdEffect effects[180];
 } BossFd; // size = 0x43A0
 
+// Boss Overhaul helpers, shared with ovl_Boss_Fd2 (see include/boss_overhaul.h)
+s16 BossFd_GetPhaseHealth(struct BossFd* this);
+s32 BossFd_IsEnraged(struct BossFd* this);
+void BossFd_SpawnBonusRock(struct BossFd* this, PlayState* play, f32 x, f32 z);
+
 #endif
diff --git a/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c b/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c
index 1a00b98..9e8e57f 100644
--- a/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c
+++ b/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.c
@@ -11,6 +11,7 @@
 #include "vt.h"
 #include "soh/frame_interpolation.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
+#include "boss_overhaul.h"
 
 #define FLAGS                                                                                 \
     (ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_HOSTILE | ACTOR_FLAG_UPDATE_CULLING_DISABLED | \
@@ -217,10 +218,11 @@ void BossFd2_SetupEmerge(BossFd2* this, PlayState* play) {
     this->actor.world.pos.x = sHoleLocations[temp_rand].x;
     this->actor.world.pos.z = sHoleLocations[temp_rand].z;
     this->work[FD2_ACTION_STATE] = 0;
+    this->work[FD2_BONUS_ATTACK] = 0;
     osSyncPrintf("UP INIT 2\n");
     this->timers[0] = 10;
     if (bossFd != NULL) {
-        health = bossFd->actor.colChkInfo.health;
+        health = BossFd_GetPhaseHealth(bossFd);
         if (health >= 18) {
             this->work[FD2_FAKEOUT_COUNT] = 0;
         } else if (health >= 12) {
@@ -257,7 +259,7 @@ void BossFd2_Emerge(BossFd2* this, PlayState* play) {
                 this->work[FD2_ACTION_STATE] = 1;
                 this->work[FD2_HOLE_COUNTER]++;
                 this->actor.world.pos.y = -200.0f;
-                health = bossFd->actor.colChkInfo.health;
+                health = BossFd_GetPhaseHealth(bossFd);
                 if (health == 24) {
                     holeTime = 30;
                 } else if (health >= 18) {
@@ -298,6 +300,16 @@ void BossFd2_Emerge(BossFd2* this, PlayState* play) {
                         this->leftMane.pos[i].z += Rand_CenteredFloat(100.0f);
                     }
                     bossFd->work[BFD_SPLASH_TIMER] = 5;
+                    if (BO_ATTACKS_ENABLED()) {
+                        // Boss Overhaul: Volvagia's roar shakes rocks loose from the ceiling above Link
+                        BossFd_SpawnBonusRock(bossFd, play, player->actor.world.pos.x, player->actor.world.pos.z);
+                        if (BossFd_IsEnraged(bossFd)) {
+                            BossFd_SpawnBonusRock(bossFd, play, player->actor.world.pos.x + Rand_CenteredFloat(200.0f),
+                                                  player->actor.world.pos.z + Rand_CenteredFloat(200.0f));
+                            BossFd_SpawnBonusRock(bossFd, play, player->actor.world.pos.x + Rand_CenteredFloat(200.0f),
+                                                  player->actor.world.pos.z + Rand_CenteredFloat(200.0f));
+                        }
+                    }
                 }
             }
             break;
@@ -323,7 +335,7 @@ void BossFd2_SetupIdle(BossFd2* this, PlayState* play) {
     osSyncPrintf("UP INIT 1\n");
     Animation_PlayLoop(&this->skelAnime, &gHoleVolvagiaTurnAnim);
     this->actionFunc = BossFd2_Idle;
-    health = bossFd->actor.colChkInfo.health;
+    health = BossFd_GetPhaseHealth(bossFd);
     if (health == 24) {
         idleTime = 50;
     } else if (health >= 18) {
@@ -335,6 +347,10 @@ void BossFd2_SetupIdle(BossFd2* this, PlayState* play) {
     } else {
         idleTime = 20;
     }
+    if (BO_ATTACKS_ENABLED()) {
+        // Boss Overhaul: less time to breathe between attacks
+        idleTime = (idleTime * 3) / 4;
+    }
     this->timers[0] = idleTime;
 }
 
@@ -384,7 +400,7 @@ void BossFd2_Burrow(BossFd2* this, PlayState* play) {
     } else {
         Math_ApproachF(&this->actor.world.pos.y, -100.0f, 1.0f, 10.0f);
         if (this->timers[0] == 0) {
-            if ((this->work[FD2_HOLE_COUNTER] >= 3) && ((s8)bossFd->actor.colChkInfo.health < 24)) {
+            if ((this->work[FD2_HOLE_COUNTER] >= 3) && (BossFd_GetPhaseHealth(bossFd) < 24)) {
                 this->work[FD2_HOLE_COUNTER] = 0;
                 this->actionFunc = BossFd2_Wait;
                 bossFd->handoffSignal = FD2_SIGNAL_FLY;
@@ -417,7 +433,13 @@ void BossFd2_BreatheFire(BossFd2* this, PlayState* play) {
 
     SkelAnime_Update(&this->skelAnime);
     if (Animation_OnFrame(&this->skelAnime, this->fwork[FD2_END_FRAME])) {
-        BossFd2_SetupBurrow(this, play);
+        if ((this->work[FD2_BONUS_ATTACK] == 0) && (bossFd != NULL) && BossFd_IsEnraged(bossFd)) {
+            // Boss Overhaul: an enraged Volvagia breathes fire a second time before burrowing
+            this->work[FD2_BONUS_ATTACK]++;
+            BossFd2_SetupBreatheFire(this, play);
+        } else {
+            BossFd2_SetupBurrow(this, play);
+        }
     }
     if ((25.0f <= this->skelAnime.curFrame) && (this->skelAnime.curFrame < 70.0f)) {
         if (this->skelAnime.curFrame == 25.0f) {
@@ -517,7 +539,13 @@ void BossFd2_ClawSwipe(BossFd2* this, PlayState* play) {
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_VALVAISA_SW_NAIL);
     }
     if (Animation_OnFrame(&this->skelAnime, this->fwork[FD2_END_FRAME])) {
-        BossFd2_SetupBurrow(this, play);
+        if (BO_ATTACKS_ENABLED() && (this->work[FD2_BONUS_ATTACK] == 0) && (this->actor.xzDistToPlayer < 250.0f)) {
+            // Boss Overhaul: if Link is still within reach, Volvagia swipes again
+            this->work[FD2_BONUS_ATTACK]++;
+            BossFd2_SetupClawSwipe(this, play);
+        } else {
+            BossFd2_SetupBurrow(this, play);
+        }
     }
 }
 
@@ -845,7 +873,7 @@ void BossFd2_CollisionCheck(BossFd2* this, PlayState* play) {
         if (!bossFd->faceExposed) {
             if (hurtbox->toucher.dmgFlags & 0x40000040) {
                 bossFd->actor.colChkInfo.health -= 2;
-                if ((s8)bossFd->actor.colChkInfo.health <= 2) {
+                if ((s8)bossFd->actor.colChkInfo.health <= BO_HP(2)) {
                     bossFd->actor.colChkInfo.health = 1;
                 }
                 bossFd->faceExposed = true;
@@ -882,7 +910,7 @@ void BossFd2_CollisionCheck(BossFd2* this, PlayState* play) {
             if (hurtbox->toucher.dmgFlags & 0x80) {
                 damage = 0;
             }
-            if (((s8)bossFd->actor.colChkInfo.health > 2) || canKill) {
+            if (((s8)bossFd->actor.colChkInfo.health > BO_HP(2)) || canKill) {
                 bossFd->actor.colChkInfo.health -= damage;
                 osSyncPrintf(VT_FGCOL(GREEN));
                 osSyncPrintf("damage   %d\n", damage);
diff --git a/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.h b/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.h
index abea186..a9e1585 100644
--- a/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.h
+++ b/soh/src/overlays/actors/ovl_Boss_Fd2/z_boss_fd2.h
@@ -44,7 +44,7 @@ typedef enum {
     /* 1 */ FD2_ACTION_STATE,
     /* 2 */ FD2_UNK_TIMER,
     /* 3 */ FD2_VAR_TIMER,
-    /* 4 */ FD2_UNUSED_4,
+    /* 4 */ FD2_BONUS_ATTACK, // Boss Overhaul: number of repeated attacks used during the current emergence
     /* 5 */ FD2_UNUSED_5,
     /* 6 */ FD2_BLINK_TIMER,
     /* 7 */ FD2_SCREAM_TIMER,
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
index 4aa8984..bb11db9 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.c
@@ -13,6 +13,7 @@
 #include "soh/frame_interpolation.h"
 #include "soh/OTRGlobals.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
+#include "boss_overhaul.h"
 
 #include <string.h>
 
@@ -353,7 +354,7 @@ void BossGanon_Init(Actor* thisx, PlayState* play2) {
         }
 
         sBossGanonGanondorf = this;
-        thisx->colChkInfo.health = 40;
+        thisx->colChkInfo.health = BO_HP(40);
         Actor_ProcessInitChain(thisx, sInitChain);
         ActorShape_Init(&thisx->shape, 0, NULL, 0);
         Actor_SetScale(thisx, 0.01f);
@@ -2013,7 +2014,15 @@ void BossGanon_PoundFloor(BossGanon* this, PlayState* play) {
             Math_ApproachF(&this->actor.velocity.y, 20.0f, 1.0f, 1.0f);
 
             if (Animation_OnFrame(&this->skelAnime, this->fwork[GDF_FWORK_1])) {
-                BossGanon_SetupWait(this, play);
+                if (BO_ATTACKS_ENABLED() && BO_BELOW_HALF((s8)this->actor.colChkInfo.health, 40) &&
+                    (this->boPoundCombo == 0)) {
+                    // Boss Overhaul: an enraged Ganondorf pounds the floor twice in a row
+                    this->boPoundCombo = 1;
+                    BossGanon_SetupPoundFloor(this, play);
+                } else {
+                    BossGanon_SetupWait(this, play);
+                    this->boPoundCombo = 0;
+                }
             }
             break;
     }
@@ -2219,6 +2228,14 @@ void BossGanon_ChargeBigMagic(BossGanon* this, PlayState* play) {
                                        this->unk_1FC.y, this->unk_1FC.z, 0, this->actor.yawTowardsPlayer, 0, 0x104 + i);
                 }
 
+                if (BO_ATTACKS_ENABLED()) {
+                    // Boss Overhaul: two more balls widen the fan (params 0x109 / 0x10B aim +/- 0x6000 to the sides)
+                    Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_BOSS_GANON, this->unk_1FC.x,
+                                       this->unk_1FC.y, this->unk_1FC.z, 0, this->actor.yawTowardsPlayer, 0, 0x109);
+                    Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_BOSS_GANON, this->unk_1FC.x,
+                                       this->unk_1FC.y, this->unk_1FC.z, 0, this->actor.yawTowardsPlayer, 0, 0x10B);
+                }
+
                 Audio_PlayActorSound2(&this->actor, NA_SE_EN_GANON_BIGMASIC);
                 Audio_PlayActorSound2(&this->actor, NA_SE_EN_GANON_THROW_BIG);
             }
@@ -2275,10 +2292,16 @@ void BossGanon_Wait(BossGanon* this, PlayState* play) {
             (fabsf(player->actor.world.pos.z) < 110.0f)) {
             BossGanon_SetupPoundFloor(this, play);
         } else if ((this->timers[0] == 0) && !(player->stateFlags1 & PLAYER_STATE1_HANGING_OFF_LEDGE)) {
-            this->timers[0] = (s16)Rand_ZeroFloat(30.0f) + 30;
+            // Boss Overhaul: shorter pauses between attacks
+            this->timers[0] = (s16)Rand_ZeroFloat(30.0f) + (BO_ATTACKS_ENABLED() ? 18 : 30);
 
-            if ((s8)this->actor.colChkInfo.health >= 20) {
-                BossGanon_SetupChargeLightBall(this, play);
+            if ((s8)this->actor.colChkInfo.health >= BO_HP(20)) {
+                if (BO_ATTACKS_ENABLED() && (Rand_ZeroOne() < 0.3f)) {
+                    // Boss Overhaul: Ganondorf also pounds the floor in the first half of the fight
+                    BossGanon_SetupPoundFloor(this, play);
+                } else {
+                    BossGanon_SetupChargeLightBall(this, play);
+                }
             } else if (Rand_ZeroOne() >= 0.5f) {
                 if ((Rand_ZeroOne() >= 0.5f) || (this->actor.xzDistToPlayer > 350.0f)) {
                     BossGanon_SetupChargeBigMagic(this, play);
@@ -2789,7 +2812,7 @@ void BossGanon_UpdateDamage(BossGanon* this, PlayState* play) {
                     hitWithSword = true;
                 }
 
-                if (((s8)this->actor.colChkInfo.health >= 3) || hitWithSword) {
+                if (((s8)this->actor.colChkInfo.health >= BO_HP(3)) || hitWithSword) {
                     this->actor.colChkInfo.health -= damage;
                 }
 
@@ -4034,7 +4057,7 @@ void BossGanon_LightBall_Update(Actor* thisx, PlayState* play2) {
                             // if ganondorf is 250 units away from link, at least 3 volleys are required
                             if ((ganondorf->actor.xyzDistToPlayerSq > 62500.0f) && (this->unk_1A4 < 3)) {
                                 this->unk_1C2 = 1;
-                            } else if (Rand_ZeroOne() < 0.7f) {
+                            } else if (Rand_ZeroOne() < (BO_ATTACKS_ENABLED() ? 0.75f : 0.7f)) {
                                 this->unk_1C2 = 1;
                             } else {
                                 this->unk_1C2 = 3;
@@ -4093,6 +4116,10 @@ void BossGanon_LightBall_Update(Actor* thisx, PlayState* play2) {
                     this->timers[1] = 2;
                     Audio_PlayActorSound2(&this->actor, NA_SE_IT_SWORD_REFLECT_MG);
                     Audio_PlayActorSound2(&this->actor, NA_SE_EN_GANON_AT_RETURN);
+                    if (BO_ATTACKS_ENABLED() && (this->actor.speedXZ < 17.0f)) {
+                        // Boss Overhaul: the ball gets faster with every volley
+                        this->actor.speedXZ += 1.5f;
+                    }
                     this->unk_1C2 = 0;
                     break;
                 }
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.h b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.h
index c92c07a..6392562 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.h
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon/z_boss_ganon.h
@@ -161,6 +161,7 @@ typedef struct BossGanon {
     /* 0x0718 */ s16 organAlpha;
     /* 0x071A */ u8 useOpenHand;
     /* 0x071B */ u8 windowShatterState;
-} BossGanon; // size = 0x71C
+    s16 boPoundCombo; // Boss Overhaul: set while Ganondorf chains a second floor pound
+} BossGanon; // size = 0x71C (vanilla)
 
 #endif
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
index e463f98..145e512 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.c
@@ -10,6 +10,7 @@
 #include "soh/OTRGlobals.h"
 #include "soh/ResourceManagerHelpers.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
+#include "boss_overhaul.h"
 
 #include <string.h>
 
@@ -56,6 +57,11 @@ const ActorInit Boss_Ganon2_InitVars = {
 
 #include "z_boss_ganon2_data.c"
 
+// Boss Overhaul: Ganon gets angrier once he is on the collapsing tower, or once he has taken real damage
+// after the first time he gets back up (health is reset to BO_HP(25) then).
+#define GANON2_ENRAGED(this) \
+    (BO_ATTACKS_ENABLED() && ((this)->unk_334 != 0 || (s8)(this)->actor.colChkInfo.health < BO_HP(25)))
+
 Vec3f D_8090EB20;
 
 EnZl3* sBossGanon2Zelda;
@@ -170,7 +176,7 @@ void BossGanon2_Init(Actor* thisx, PlayState* play) {
     }
 
     this->actor.colChkInfo.mass = MASS_IMMOVABLE;
-    this->actor.colChkInfo.health = 30;
+    this->actor.colChkInfo.health = BO_HP(30);
     Collider_InitJntSph(play, &this->unk_424);
     Collider_SetJntSph(play, &this->unk_424, &this->actor, &sJntSphInit1, this->unk_464);
     Collider_InitJntSph(play, &this->unk_444);
@@ -1093,9 +1099,9 @@ void func_808FFDB0(BossGanon2* this, PlayState* play) {
         this->actionFunc = func_808FFEBC;
 
         if (this->unk_334 != 0) {
-            this->unk_1A2[0] = Rand_ZeroFloat(30.0f);
+            this->unk_1A2[0] = Rand_ZeroFloat(BO_ATTACKS_ENABLED() ? 16.0f : 30.0f);
         } else {
-            this->unk_1A2[0] = 40;
+            this->unk_1A2[0] = BO_ATTACKS_ENABLED() ? (GANON2_ENRAGED(this) ? 20 : 28) : 40;
         }
 
         this->unk_336 = 1;
@@ -1204,7 +1210,7 @@ void func_809002CC(BossGanon2* this, PlayState* play) {
     Animation_MorphToLoop(&this->skelAnime, &gGanonGuardWalkAnim, -10.0f);
     this->actionFunc = func_80900344;
     this->unk_338 = 0;
-    this->unk_1A2[0] = 100;
+    this->unk_1A2[0] = BO_ATTACKS_ENABLED() ? 70 : 100;
     this->unk_390 = (s16)Rand_ZeroFloat(50.0f) + 50;
 }
 
@@ -1236,9 +1242,12 @@ void func_80900344(BossGanon2* this, PlayState* play) {
                 this->skelAnime.playSpeed *= 1.5f;
             }
         }
+        if (GANON2_ENRAGED(this)) {
+            this->skelAnime.playSpeed *= 1.25f;
+        }
         phi_f0 = this->skelAnime.playSpeed * 3.0f;
     } else {
-        phi_f0 = 2.0f;
+        phi_f0 = BO_ATTACKS_ENABLED() ? (GANON2_ENRAGED(this) ? 3.0f : 2.5f) : 2.0f;
         if (this->actor.xzDistToPlayer >= 200.0f) {
             this->unk_338 = 1;
             Animation_MorphToLoop(&this->skelAnime, &gGanonWalkAnim, -10.0f);
@@ -1269,6 +1278,10 @@ void func_80900580(BossGanon2* this, PlayState* play) {
         this->unk_198 = (this->unk_194 - 15.0f) - 5.0f;
     }
 
+    if (GANON2_ENRAGED(this)) {
+        this->skelAnime.playSpeed = 1.25f;
+    }
+
     this->actionFunc = func_80900650;
 }
 
@@ -1297,7 +1310,13 @@ void func_80900650(BossGanon2* this, PlayState* play) {
 
         if ((this->unk_311 == 1) && (this->actor.xzDistToPlayer < 250.0f) && this->unk_313) {
             func_80900580(this, play);
+        } else if ((this->unk_311 == 0) && GANON2_ENRAGED(this) && (this->boSwingCombo == 0) &&
+                   (this->actor.xzDistToPlayer < 250.0f) && this->unk_313) {
+            // Enraged: one more left/right pair of swings tacked onto the chain.
+            this->boSwingCombo = 1;
+            func_80900580(this, play);
         } else {
+            this->boSwingCombo = 0;
             func_808FFDB0(this, play);
         }
     }
@@ -1464,7 +1483,7 @@ void func_80900890(BossGanon2* this, PlayState* play) {
             if (Animation_OnFrame(&this->skelAnime, this->unk_194)) {
                 func_808FFDB0(this, play);
                 if (this->unk_334 == 0) {
-                    this->actor.colChkInfo.health = 25;
+                    this->actor.colChkInfo.health = BO_HP(25);
                 }
                 this->unk_336 = 1;
             }
@@ -1961,7 +1980,7 @@ void func_80902524(BossGanon2* this, PlayState* play) {
                     Audio_StopSfxById(NA_SE_EN_MGANON_UNARI);
                     this->actor.colChkInfo.health -= 2;
                     temp_v0_4 = this->actor.colChkInfo.health;
-                    if (temp_v0_4 < 0x15 && this->unk_334 == 0) {
+                    if (temp_v0_4 < BO_HP(0x15) && this->unk_334 == 0) {
                         func_80900818(this, play);
                     } else {
                         if (temp_v0_4 <= 0) {
@@ -1995,7 +2014,7 @@ void func_80902524(BossGanon2* this, PlayState* play) {
             }
             this->actor.colChkInfo.health -= phi_v1_2;
             temp_v0_4 = this->actor.colChkInfo.health;
-            if ((temp_v0_4 < 0x15) && (this->unk_334 == 0)) {
+            if ((temp_v0_4 < BO_HP(0x15)) && (this->unk_334 == 0)) {
                 func_80900818(this, play);
             } else if ((temp_v0_4 <= 0) && (phi_v1_2 >= 2)) {
                 func_80901020(this, play);
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.h b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.h
index 43539c1..23769b5 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.h
+++ b/soh/src/overlays/actors/ovl_Boss_Ganon2/z_boss_ganon2.h
@@ -106,6 +106,8 @@ typedef struct BossGanon2 {
     /* 0x0444 */ ColliderJntSph unk_444;
     /* 0x0464 */ ColliderJntSphElement unk_464[16];
     /* 0x0864 */ ColliderJntSphElement unk_864[2];
-} BossGanon2; // size = 0x08E4
+    // Boss Overhaul: extra swing chain marker
+    s16 boSwingCombo;
+} BossGanon2; // size = 0x08E4 (+ overhaul field)
 
 #endif
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.c b/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.c
index 984da2d..e963a52 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.c
+++ b/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.c
@@ -13,6 +13,7 @@
 #include "overlays/actors/ovl_Door_Warp1/z_door_warp1.h"
 #include "soh/OTRGlobals.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
+#include "boss_overhaul.h"
 
 #define FLAGS                                                                                 \
     (ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_HOSTILE | ACTOR_FLAG_UPDATE_CULLING_DISABLED | \
@@ -220,7 +221,7 @@ void BossGanondrof_Init(Actor* thisx, PlayState* play) {
     SkelAnime_Init(play, &this->skelAnime, &gPhantomGanonSkel, &gPhantomGanonRideAnim, NULL, NULL, 0);
     if (this->actor.params < GND_FAKE_BOSS) {
         this->actor.params = GND_REAL_BOSS;
-        this->actor.colChkInfo.health = 30;
+        this->actor.colChkInfo.health = BO_HP(30);
         this->lightNode = LightContext_InsertLight(play, &play->lightCtx, &this->lightInfo);
         Lights_PointNoGlowSetInfo(&this->lightInfo, this->actor.world.pos.x, this->actor.world.pos.y,
                                   this->actor.world.pos.z, 255, 255, 255, 255);
@@ -442,6 +443,90 @@ void BossGanondrof_SetupNeutral(BossGanondrof* this, f32 arg1) {
     this->timers[0] = (s16)(Rand_ZeroOne() * 64.0f) + 30;
 }
 
+// Boss Overhaul: Phantom Ganon switches to his "final" attack pattern (charges, faster volleys) once he is at half
+// health instead of only when he is nearly defeated.
+static s32 BossGanondrof_UseFinalPattern(BossGanondrof* this) {
+    s32 health = (s8)this->actor.colChkInfo.health;
+
+    if (health < BO_HP(5)) {
+        return true;
+    }
+
+    return BO_ATTACKS_ENABLED() && health <= BO_HP(30) / 2;
+}
+
+// Boss Overhaul: Phantom Ganon calls down a lightning storm once he has lost a third of his health.
+static s32 BossGanondrof_ShouldStartStorm(BossGanondrof* this, f32 rand01) {
+    return BO_ATTACKS_ENABLED() && this->actor.params == GND_REAL_BOSS && this->stormTimer == 0 &&
+           (s8)this->actor.colChkInfo.health <= (BO_HP(30) * 2) / 3 && rand01 < 0.3f;
+}
+
+static void BossGanondrof_StartStorm(BossGanondrof* this, PlayState* play) {
+    Player* player = GET_PLAYER(play);
+    s16 baseAngle = (s16)Rand_CenteredFloat(65535.0f);
+    s16 i;
+
+    this->stormCount = ((s8)this->actor.colChkInfo.health <= BO_HP(10)) ? 3 : 2;
+    this->stormTimer = 44;
+
+    for (i = 0; i < this->stormCount; i++) {
+        f32 radius = (i == 0) ? 0.0f : 130.0f;
+        f32 x = player->actor.world.pos.x + Math_SinS(baseAngle + i * 0x5555) * radius;
+        f32 z = player->actor.world.pos.z + Math_CosS(baseAngle + i * 0x5555) * radius;
+        f32 dx = x - GND_BOSSROOM_CENTER_X;
+        f32 dz = z - GND_BOSSROOM_CENTER_Z;
+        f32 dist = sqrtf(SQ(dx) + SQ(dz));
+
+        if (dist > 250.0f) {
+            x = GND_BOSSROOM_CENTER_X + dx * (250.0f / dist);
+            z = GND_BOSSROOM_CENTER_Z + dz * (250.0f / dist);
+        }
+
+        this->stormPos[i].x = x;
+        this->stormPos[i].y = player->actor.floorHeight;
+        this->stormPos[i].z = z;
+    }
+
+    Audio_PlayActorSound2(&this->actor, NA_SE_EN_FANTOM_LAUGH);
+}
+
+static void BossGanondrof_UpdateStorm(BossGanondrof* this, PlayState* play) {
+    s16 i;
+
+    if (this->stormTimer == 0) {
+        return;
+    }
+
+    if (this->actionFunc == BossGanondrof_Death || this->deathState != NOT_DEAD) {
+        this->stormTimer = 0;
+        return;
+    }
+
+    this->stormTimer--;
+
+    for (i = 0; i < this->stormCount; i++) {
+        Vec3f pos = this->stormPos[i];
+        Vec3f vel = { 0.0f, 0.0f, 0.0f };
+        Vec3f accel = { 0.0f, -0.3f, 0.0f };
+
+        if ((this->stormTimer & 1) == 0) {
+            // light gathers where the lightning is about to strike
+            pos.x += Rand_CenteredFloat(50.0f);
+            pos.z += Rand_CenteredFloat(50.0f);
+            vel.x = Rand_CenteredFloat(6.0f);
+            vel.y = Rand_ZeroFloat(6.0f) + 2.0f;
+            vel.z = Rand_CenteredFloat(6.0f);
+            EffectSsFhgFlash_SpawnLightBall(play, &pos, &vel, &accel, (s16)(Rand_ZeroOne() * 60.0f) + 150,
+                                            FHGFLASH_LIGHTBALL_GREEN);
+        }
+
+        if (this->stormTimer == (24 - i * 12)) {
+            Actor_Spawn(&play->actorCtx, play, ACTOR_EN_FHG_FIRE, this->stormPos[i].x, this->stormPos[i].y + 225.0f,
+                        this->stormPos[i].z, 0, 0, 0, FHGFIRE_LIGHTNING_STRIKE);
+        }
+    }
+}
+
 void BossGanondrof_Neutral(BossGanondrof* this, PlayState* play) {
     f32 targetX;
     f32 targetY;
@@ -458,7 +543,9 @@ void BossGanondrof_Neutral(BossGanondrof* this, PlayState* play) {
             if (this->timers[0] == 0) {
                 this->timers[0] = (s16)(Rand_ZeroOne() * 64.0f) + 30;
                 rand01 = Rand_ZeroOne();
-                if (thisx->colChkInfo.health < 5) {
+                if (BossGanondrof_ShouldStartStorm(this, rand01)) {
+                    BossGanondrof_StartStorm(this, play);
+                } else if (BossGanondrof_UseFinalPattern(this)) {
                     if (rand01 < 0.25f) {
                         BossGanondrof_SetupThrow(this, play);
                     } else if (rand01 >= 0.8f) {
@@ -1219,7 +1306,7 @@ void BossGanondrof_CollisionCheck(BossGanondrof* this, PlayState* play) {
                         }
                         dmg = CollisionCheck_GetSwordDamage(dmgFlags, play);
                         (dmg == 0) ? (dmg = 2) : (canKill = true);
-                        if (((s8)this->actor.colChkInfo.health > 2) || canKill) {
+                        if (((s8)this->actor.colChkInfo.health > BO_HP(2)) || canKill) {
                             this->actor.colChkInfo.health -= dmg;
                         }
 
@@ -1275,6 +1362,7 @@ void BossGanondrof_Update(Actor* thisx, PlayState* play) {
     osSyncPrintf("MOVE START EEEEEEEEEEEEEEEEEEEEEE%d\n", this->actor.params);
 
     this->actionFunc(this, play);
+    BossGanondrof_UpdateStorm(this, play);
 
     for (i = 0; i < ARRAY_COUNT(this->timers); i++) {
         if (this->timers[i]) {
diff --git a/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.h b/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.h
index 1e45ca7..11d0724 100644
--- a/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.h
+++ b/soh/src/overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.h
@@ -116,6 +116,10 @@ typedef struct BossGanondrof {
     /* 0x04D0 */ LightInfo lightInfo;
     /* 0x04E0 */ ColliderCylinder colliderBody;
     /* 0x052C */ ColliderCylinder colliderSpear;
+    // Boss Overhaul: lightning storm attack
+    s16 stormTimer;
+    s16 stormCount;
+    Vec3f stormPos[3];
 } BossGanondrof; // size = 0x0578
 
 #endif
diff --git a/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.c b/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.c
index 3920cca..2d79ea2 100644
--- a/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.c
+++ b/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.c
@@ -7,6 +7,7 @@
 #include "soh/OTRGlobals.h"
 #include "soh/Enhancements/game-interactor/GameInteractor.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
+#include "boss_overhaul.h"
 
 #define FLAGS                                                                                 \
     (ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_HOSTILE | ACTOR_FLAG_UPDATE_CULLING_DISABLED | \
@@ -29,6 +30,16 @@ typedef enum {
     VISUALSTATE_HIT          // main: greenish cyan, alternates with red; eye: greenish cyan
 } GohmaVisualState;
 
+// Boss Overhaul: number of baby gohmas that hang from Gohma's tail during the ceiling phase.
+static s16 BossGoma_GetChildCount(void) {
+    return BO_ATTACKS_ENABLED() ? 5 : 3;
+}
+
+// Boss Overhaul: Gohma is enraged once it is down to half of its (possibly doubled) health.
+static s32 BossGoma_IsEnraged(BossGoma* this) {
+    return BO_ATTACKS_ENABLED() && (s8)this->actor.colChkInfo.health <= BO_HP(10) / 2;
+}
+
 void BossGoma_Init(Actor* thisx, PlayState* play);
 void BossGoma_Destroy(Actor* thisx, PlayState* play);
 void BossGoma_Update(Actor* thisx, PlayState* play);
@@ -336,7 +347,7 @@ void BossGoma_Init(Actor* thisx, PlayState* play) {
     this->actor.world.pos.y = -300.0f; // ceiling
     this->actor.gravity = 0.0f;
     BossGoma_SetupEncounter(this, play);
-    this->actor.colChkInfo.health = 10;
+    this->actor.colChkInfo.health = BO_HP(10);
     this->actor.colChkInfo.mass = MASS_IMMOVABLE;
     Collider_InitJntSph(play, &this->collider);
     Collider_SetJntSph(play, &this->collider, &this->actor, &sColliderJntSphInit, this->colliderItems);
@@ -432,7 +443,7 @@ void BossGoma_SetupEncounter(BossGoma* this, PlayState* play) {
 void BossGoma_SetupFloorIdle(BossGoma* this) {
     f32 lastFrame = Animation_GetLastFrame(&gGohmaIdleCrouchedAnim);
 
-    this->framesUntilNextAction = Rand_S16Offset(20, 30);
+    this->framesUntilNextAction = BossGoma_IsEnraged(this) ? Rand_S16Offset(8, 14) : Rand_S16Offset(20, 30);
     Animation_Change(&this->skelanime, &gGohmaIdleCrouchedAnim, 1.0f, 0.0f, lastFrame, ANIMMODE_LOOP, -5.0f);
     this->actionFunc = BossGoma_FloorIdle;
 }
@@ -454,7 +465,9 @@ void BossGoma_SetupFallJump(BossGoma* this) {
     // When in Enemy Randomizer, reset the state of the spawned Gohma Larva because it's not done
     // by the (non-existent) Larva themselves.
     if (CVarGetInteger(CVAR_ENHANCEMENT("RandomizedEnemies"), 0)) {
-        this->childrenGohmaState[0] = this->childrenGohmaState[1] = this->childrenGohmaState[2] = 0;
+        for (s16 j = 0; j < ARRAY_COUNT(this->childrenGohmaState); j++) {
+            this->childrenGohmaState[j] = 0;
+        }
     }
     Animation_Change(&this->skelanime, &gGohmaLandAnim, 1.0f, 0.0f, 0.0f, ANIMMODE_ONCE, -5.0f);
     this->actionFunc = BossGoma_FallJump;
@@ -1318,6 +1331,17 @@ void BossGoma_FloorAttack(BossGoma* this, PlayState* play) {
                 }
             }
 
+            if (BO_ATTACKS_ENABLED()) {
+                // Boss Overhaul: Gohma lunges at the player before slamming down
+                if (this->skelanime.curFrame < 10.0f) {
+                    Math_ApproachF(&this->actor.speedXZ, BossGoma_IsEnraged(this) ? 16.0f : 12.0f, 0.5f, 4.0f);
+                    Math_ApproachS(&this->actor.world.rot.y, Actor_WorldYawTowardActor(&this->actor, &GET_PLAYER(play)->actor),
+                                   2, 0x800);
+                } else {
+                    Math_ApproachZeroF(&this->actor.speedXZ, 0.5f, 3.0f);
+                }
+            }
+
             if (Animation_OnFrame(&this->skelanime, 10.0f)) {
                 BossGoma_PlayEffectsAndSfx(this, play, 3, 5);
                 func_80033E88(&this->actor, play, 5, 15);
@@ -1385,7 +1409,7 @@ void BossGoma_FloorLandStruckDown(BossGoma* this, PlayState* play) {
         BossGoma_SetupFloorStunned(this);
         this->sfxFaintTimer = 92;
         this->patienceTimer = 0;
-        this->framesUntilNextAction = 150;
+        this->framesUntilNextAction = BossGoma_IsEnraged(this) ? 110 : 150;
     }
 
     Actor_SpawnFloorDustRing(play, &this->actor, &this->actor.world.pos, 55.0f, 4, 8.0f, 500, 10, true);
@@ -1400,7 +1424,7 @@ void BossGoma_FloorLand(BossGoma* this, PlayState* play) {
 
     if (Animation_OnFrame(&this->skelanime, this->currentAnimFrameCount)) {
         BossGoma_SetupFloorIdle(this);
-        this->patienceTimer = 200;
+        this->patienceTimer = BossGoma_IsEnraged(this) ? 300 : 200;
     }
 }
 
@@ -1502,14 +1526,23 @@ void BossGoma_CeilingSpawnGohmas(BossGoma* this, PlayState* play) {
     }
 
     if (this->tailLimbsScaleTimers[0] == 2) {
-        for (i = 0; i < ARRAY_COUNT(this->childrenGohmaState); i++) {
+        s16 childCount = BossGoma_GetChildCount();
+        s16 unspawnedLeft = false;
+
+        for (i = 0; i < childCount; i++) {
             if (this->childrenGohmaState[i] == 0) {
                 BossGoma_SpawnChildGohma(this, play, i);
                 break;
             }
         }
 
-        if (this->childrenGohmaState[0] == 0 || this->childrenGohmaState[1] == 0 || this->childrenGohmaState[2] == 0) {
+        for (i = 0; i < childCount; i++) {
+            if (this->childrenGohmaState[i] == 0) {
+                unspawnedLeft = true;
+            }
+        }
+
+        if (unspawnedLeft) {
             this->spawnGohmasActionTimer = 23;
         }
     }
@@ -1565,17 +1598,28 @@ void BossGoma_CeilingIdle(BossGoma* this, PlayState* play) {
         if (CVarGetInteger(CVAR_ENHANCEMENT("RandomizedEnemies"), 0)) {
             nearbyEnTest = Actor_FindNearby(play, &this->actor, -1, ACTORCAT_ENEMY, 8000.0f);
         }
-        if (this->childrenGohmaState[0] == 0 && this->childrenGohmaState[1] == 0 && this->childrenGohmaState[2] == 0) {
+        s16 childCount = BossGoma_GetChildCount();
+        s16 noneSpawned = true;
+        s16 allDead = true;
+
+        for (i = 0; i < childCount; i++) {
+            if (this->childrenGohmaState[i] != 0) {
+                noneSpawned = false;
+            }
+            if (this->childrenGohmaState[i] >= 0) {
+                allDead = false;
+            }
+        }
+
+        if (noneSpawned) {
             // if no child gohma has been spawned
             BossGoma_SetupCeilingPrepareSpawnGohmas(this);
-        } else if ((this->childrenGohmaState[0] < 0 && this->childrenGohmaState[1] < 0 &&
-                    this->childrenGohmaState[2] < 0) ||
-                   (nearbyEnTest == NULL && CVarGetInteger(CVAR_ENHANCEMENT("RandomizedEnemies"), 0))) {
+        } else if (allDead || (nearbyEnTest == NULL && CVarGetInteger(CVAR_ENHANCEMENT("RandomizedEnemies"), 0))) {
             // In authentic gameplay, check if all baby Ghomas are dead. In Enemy Randomizer, check if there's no
             // enemies alive.
             BossGoma_SetupFallJump(this);
         } else {
-            for (i = 0; i < ARRAY_COUNT(this->childrenGohmaState); i++) {
+            for (i = 0; i < childCount; i++) {
                 if (this->childrenGohmaState[i] == 0) {
                     // if any child gohma hasn't been spawned
                     // this seems unreachable since BossGoma_CeilingSpawnGohmas spawns all three and can't be
@@ -1626,11 +1670,11 @@ void BossGoma_FloorMain(BossGoma* this, PlayState* play) {
         if (this->patienceTimer != 0) {
             this->patienceTimer--;
 
-            if (this->actor.xzDistToPlayer < 150.0f) {
+            if (this->actor.xzDistToPlayer < (BO_ATTACKS_ENABLED() ? 210.0f : 150.0f)) {
                 BossGoma_SetupFloorAttackPosture(this);
             }
 
-            Math_ApproachF(&this->actor.speedXZ, 10.0f / 3.0f, 0.5f, 2.0f);
+            Math_ApproachF(&this->actor.speedXZ, BossGoma_IsEnraged(this) ? 5.0f : 10.0f / 3.0f, 0.5f, 2.0f);
             Math_ApproachS(&this->actor.world.rot.y, rot, 5, 0x3E8);
         } else {
             if (this->timer != 0) {
@@ -1682,7 +1726,9 @@ void BossGoma_WallClimb(BossGoma* this, PlayState* play) {
     if (this->actor.world.pos.y > -320.0f) {
         BossGoma_SetupCeilingMoveToCenter(this);
         // allow new spawns
-        this->childrenGohmaState[0] = this->childrenGohmaState[1] = this->childrenGohmaState[2] = 0;
+        for (s16 j = 0; j < ARRAY_COUNT(this->childrenGohmaState); j++) {
+            this->childrenGohmaState[j] = 0;
+        }
     }
 }
 
@@ -1749,8 +1795,11 @@ void BossGoma_UpdateEye(BossGoma* this, PlayState* play) {
             }
         }
 
-        if (this->childrenGohmaState[0] > 0 || this->childrenGohmaState[1] > 0 || this->childrenGohmaState[2] > 0) {
-            this->eyeClosedTimer = 7;
+        for (s16 j = 0; j < ARRAY_COUNT(this->childrenGohmaState); j++) {
+            if (this->childrenGohmaState[j] > 0) {
+                this->eyeClosedTimer = 7;
+                break;
+            }
         }
 
         if (this->eyeClosedTimer != 0) {
@@ -1859,9 +1908,9 @@ void BossGoma_UpdateHit(BossGoma* this, PlayState* play) {
                 this->sfxFaintTimer = 100;
 
                 if (acHitInfo->toucher.dmgFlags & 1) {
-                    this->framesUntilNextAction = 40;
+                    this->framesUntilNextAction = BossGoma_IsEnraged(this) ? 28 : 40;
                 } else {
-                    this->framesUntilNextAction = 90;
+                    this->framesUntilNextAction = BossGoma_IsEnraged(this) ? 65 : 90;
                 }
 
                 this->timer = 4;
@@ -2186,7 +2235,7 @@ void BossGoma_Draw(Actor* thisx, PlayState* play) {
 
 void BossGoma_SpawnChildGohma(BossGoma* this, PlayState* play, s16 i) {
     Actor_SpawnAsChild(&play->actorCtx, &this->actor, play, ACTOR_EN_GOMA, this->lastTailLimbWorldPos.x,
-                       this->lastTailLimbWorldPos.y - 50.0f, this->lastTailLimbWorldPos.z, 0, i * (0x10000 / 3), 0, i);
+                       this->lastTailLimbWorldPos.y - 50.0f, this->lastTailLimbWorldPos.z, 0, i * (0x10000 / BossGoma_GetChildCount()), 0, i);
 
     this->childrenGohmaState[i] = 1;
 }
diff --git a/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.h b/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.h
index c342e0b..259b45c 100644
--- a/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.h
+++ b/soh/src/overlays/actors/ovl_Boss_Goma/z_boss_goma.h
@@ -110,7 +110,7 @@ typedef struct BossGoma {
     /* 0x019E */ s16 eyeIrisRotX;
     /* 0x01A0 */ s16 eyeIrisRotY;
     /* 0x01A2 */ s16 unusedTimer;
-    /* 0x01A4 */ s16 childrenGohmaState[3]; // 0 not spawned, 1 spawned, -1 dead (-1 set by child gohma)
+    /* 0x01A4 */ s16 childrenGohmaState[6]; // 0 not spawned, 1 spawned, -1 dead (-1 set by child gohma); only the first 3 (5 with Boss Overhaul) are used
     /* 0x01AA */ s16 tailLimbsScaleTimers[4];
     /* 0x01B2 */ s16 spawnGohmasActionTimer;
     /* 0x01B4 */ s16 eyeState;
diff --git a/soh/src/overlays/actors/ovl_Boss_Mo/z_boss_mo.c b/soh/src/overlays/actors/ovl_Boss_Mo/z_boss_mo.c
index a13eb15..9e2ad04 100644
--- a/soh/src/overlays/actors/ovl_Boss_Mo/z_boss_mo.c
+++ b/soh/src/overlays/actors/ovl_Boss_Mo/z_boss_mo.c
@@ -14,6 +14,7 @@
 #include "soh/frame_interpolation.h"
 #include "soh/Enhancements/game-interactor/GameInteractor.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
+#include "boss_overhaul.h"
 
 #include <string.h>
 
@@ -146,6 +147,10 @@ static BossMo* sMorphaCore = NULL;
 static BossMo* sMorphaTent1 = NULL;
 static BossMo* sMorphaTent2 = NULL;
 
+// Boss Overhaul: Morpha is enraged when its core is at or below half health (and extra attacks are enabled).
+#define MO_ENRAGED() (BO_ATTACKS_ENABLED() && (sMorphaCore != NULL) && \
+                      BO_BELOW_HALF((s8)sMorphaCore->actor.colChkInfo.health, 20))
+
 static f32 sFlatWidth[41] = {
     15.0f, 12.0f, 9.0f, 6.5f, 4.8f, 4.0f, 3.4f, 3.1f, 3.0f, 3.1f, 3.2f, 3.4f, 3.6f, 3.8f,
     4.0f,  4.6f,  5.1f, 5.5f, 6.1f, 6.6f, 7.3f, 7.7f, 8.4f, 8.5f, 8.7f, 8.8f, 8.8f, 8.7f,
@@ -365,7 +370,7 @@ void BossMo_Init(Actor* thisx, PlayState* play2) {
         this->actor.world.pos.y = MO_WATER_LEVEL(play) + 50.0f;
         this->fwork[MO_TENT_SWING_SIZE_X] = 5.0f;
         this->drawActor = true;
-        this->actor.colChkInfo.health = 20;
+        this->actor.colChkInfo.health = BO_HP(20);
         this->actor.colChkInfo.mass = 0;
         this->actor.params = 0;
         Actor_SetScale(&this->actor, 0.01f);
@@ -654,9 +659,10 @@ void BossMo_Tentacle(BossMo* this, PlayState* play) {
                 Math_ApproachF(&this->tentSpeed, 400.0f, 1.0f, 6.0f);
             }
             if (this->work[MO_TENT_ACTION_STATE] == MO_TENT_READY) {
-                if ((this->timers[0] == 0) && !HAS_LINK(otherTent)) {
+                // Boss Overhaul: less downtime between tentacle attacks
+                if ((this->timers[0] <= (BO_ATTACKS_ENABLED() ? (MO_ENRAGED() ? 25 : 15) : 0)) && !HAS_LINK(otherTent)) {
                     this->work[MO_TENT_ACTION_STATE] = MO_TENT_SWING;
-                    this->timers[0] = 50;
+                    this->timers[0] = BO_ATTACKS_ENABLED() ? 36 : 50;
                     Audio_ResetIncreasingTranspose();
                     this->attackAngleMod = Rand_CenteredFloat(0x1000);
                 }
@@ -969,7 +975,8 @@ void BossMo_Tentacle(BossMo* this, PlayState* play) {
                     }
                 }
             }
-            if ((this == sMorphaTent1) && (sMorphaCore->hitCount >= 3) && (sMorphaTent2 == NULL)) {
+            if ((this == sMorphaTent1) && (sMorphaCore->hitCount >= (BO_ATTACKS_ENABLED() ? 2 : 3)) &&
+        (sMorphaTent2 == NULL)) {
                 sMorphaTent2 =
                     (BossMo*)Actor_Spawn(&play->actorCtx, play, ACTOR_BOSS_MO, this->actor.world.pos.x,
                                          this->actor.world.pos.y, this->actor.world.pos.z, 0, 0, 0, BOSSMO_TENTACLE);
@@ -2010,7 +2017,7 @@ void BossMo_Core(BossMo* this, PlayState* play) {
                 this->work[MO_CORE_POS_IN_TENT]--;
                 if (this->work[MO_CORE_POS_IN_TENT] <= 0) {
                     this->work[MO_TENT_ACTION_STATE] = MO_CORE_MAKE_TENT;
-                    this->timers[0] = 100;
+                    this->timers[0] = BO_ATTACKS_ENABLED() ? 70 : 100;
                     this->tentSpeed = 0.0f;
                     this->actor.speedXZ = 0.0f;
                 }
@@ -2060,9 +2067,34 @@ void BossMo_Core(BossMo* this, PlayState* play) {
             case MO_CORE_MOVE:
                 sp80 = Math_SinS(this->work[MO_TENT_VAR_TIMER] * 0x800) * 100.0f;
                 sp7C = Math_CosS(this->work[MO_TENT_VAR_TIMER] * 0x800) * 100.0f;
-                Math_ApproachF(&this->actor.world.pos.x, sMorphaTent1->targetPos.x + sp80, 0.05f, this->actor.speedXZ);
-                Math_ApproachF(&this->actor.world.pos.z, sMorphaTent1->targetPos.z + sp7C, 0.05f, this->actor.speedXZ);
-                Math_ApproachF(&this->actor.speedXZ, 10.0f, 1.0f, 0.5f);
+                if (BO_ATTACKS_ENABLED()) {
+                    // Boss Overhaul: the core moves faster, and once enraged it periodically lunges straight at Link.
+                    f32 targetX = sMorphaTent1->targetPos.x + sp80;
+                    f32 targetZ = sMorphaTent1->targetPos.z + sp7C;
+                    f32 approachScale = 0.05f;
+                    f32 targetSpeed = MO_ENRAGED() ? 15.0f : 13.0f;
+
+                    if (MO_ENRAGED()) {
+                        if (this->timers[2] == 0) {
+                            this->timers[2] = 140;
+                        }
+                        if ((this->timers[2] > 120) && (player->actor.world.pos.y >= MO_WATER_LEVEL(play))) {
+                            targetX = player->actor.world.pos.x;
+                            targetZ = player->actor.world.pos.z;
+                            approachScale = 0.15f;
+                            targetSpeed = 26.0f;
+                        }
+                    }
+                    Math_ApproachF(&this->actor.world.pos.x, targetX, approachScale, this->actor.speedXZ);
+                    Math_ApproachF(&this->actor.world.pos.z, targetZ, approachScale, this->actor.speedXZ);
+                    Math_ApproachF(&this->actor.speedXZ, targetSpeed, 1.0f, 1.5f);
+                } else {
+                    Math_ApproachF(&this->actor.world.pos.x, sMorphaTent1->targetPos.x + sp80, 0.05f,
+                                   this->actor.speedXZ);
+                    Math_ApproachF(&this->actor.world.pos.z, sMorphaTent1->targetPos.z + sp7C, 0.05f,
+                                   this->actor.speedXZ);
+                    Math_ApproachF(&this->actor.speedXZ, 10.0f, 1.0f, 0.5f);
+                }
                 break;
             case MO_CORE_STUNNED:
                 this->actor.velocity.x = Math_SinS(this->actor.world.rot.y) * this->actor.speedXZ;
diff --git a/soh/src/overlays/actors/ovl_Boss_Sst/z_boss_sst.c b/soh/src/overlays/actors/ovl_Boss_Sst/z_boss_sst.c
index aa68a58..66cfcbd 100644
--- a/soh/src/overlays/actors/ovl_Boss_Sst/z_boss_sst.c
+++ b/soh/src/overlays/actors/ovl_Boss_Sst/z_boss_sst.c
@@ -14,6 +14,7 @@
 #include "soh/ResourceManagerHelpers.h"
 #include "soh/Enhancements/game-interactor/GameInteractor.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
+#include "boss_overhaul.h"
 
 #define FLAGS                                                                                 \
     (ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_HOSTILE | ACTOR_FLAG_UPDATE_CULLING_DISABLED | \
@@ -200,6 +201,10 @@ static BossSst* sHead;
 static BossSst* sHands[2];
 static BgSstFloor* sFloor;
 
+// Boss Overhaul: Bongo Bongo is enraged when its head is at or below half health (and extra attacks are enabled).
+#define BONGO_ENRAGED() \
+    (BO_ATTACKS_ENABLED() && (sHead != NULL) && BO_BELOW_HALF((s8)sHead->actor.colChkInfo.health, 36))
+
 static Vec3f sRoomCenter = { ROOM_CENTER_X, ROOM_CENTER_Y, ROOM_CENTER_Z };
 static Vec3f sHandOffsets[2];
 static s16 sHandYawOffsets[2];
@@ -280,6 +285,9 @@ void BossSst_Init(Actor* thisx, PlayState* play2) {
     Collider_InitCylinder(play, &this->colliderCyl);
     Collider_InitJntSph(play, &this->colliderJntSph);
     CollisionCheck_SetInfo(&this->actor.colChkInfo, &sDamageTable, &sColChkInfoInit);
+    if (this->actor.params == BONGO_HEAD) {
+        this->actor.colChkInfo.health = BO_HP(36);
+    }
     Flags_SetSwitch(play, 0x14);
     if (this->actor.params == BONGO_HEAD) {
         sFloor = (BgSstFloor*)Actor_Spawn(&play->actorCtx, play, ACTOR_BG_SST_FLOOR, sRoomCenter.x, sRoomCenter.y,
@@ -653,7 +661,8 @@ void BossSst_HeadWait(BossSst* this, PlayState* play) {
 }
 
 void BossSst_HeadSetupNeutral(BossSst* this) {
-    this->timer = 127;
+    // Boss Overhaul: shorter pauses between attacks (the values keep the drum-beat timing of the hands intact)
+    this->timer = BONGO_ENRAGED() ? 71 : (BO_ATTACKS_ENABLED() ? 99 : 127);
     this->ready = false;
     this->actionFunc = BossSst_HeadNeutral;
 }
@@ -676,6 +685,11 @@ void BossSst_HeadNeutral(BossSst* this, PlayState* play) {
             !(GET_PLAYER(play)->stateFlags1 &
               (PLAYER_STATE1_DEAD | PLAYER_STATE1_HANGING_OFF_LEDGE | PLAYER_STATE1_CLIMBING_LEDGE))) {
             sHands[Rand_ZeroOne() <= 0.5f]->ready = true;
+            if (BO_ATTACKS_ENABLED() && (Rand_ZeroOne() < (BONGO_ENRAGED() ? 0.7f : 0.3f))) {
+                // Boss Overhaul: sometimes both hands attack in quick succession
+                sHands[LEFT]->ready = true;
+                sHands[RIGHT]->ready = true;
+            }
             BossSst_HeadSetupWait(this);
         } else {
             this->timer = 28;
@@ -1477,7 +1491,7 @@ void BossSst_HandReadySlam(BossSst* this, PlayState* play) {
 
         if (Math_StepToF(&this->actor.world.pos.y, ROOM_CENTER_Y + 300.0f, 30.0f) &&
             (this->actor.xzDistToPlayer < 140.0f)) {
-            this->timer = 20;
+            this->timer = BO_ATTACKS_ENABLED() ? 12 : 20;
         }
         Math_ScaledStepToS(&this->actor.shape.rot.x, -0x1000, 0x100);
         Math_ApproachF(&this->actor.world.pos.x, player->actor.world.pos.x, 0.5f, 40.0f);
@@ -1506,6 +1520,15 @@ void BossSst_HandSlam(BossSst* this, PlayState* play) {
             this->timer--;
         }
 
+        if (BO_ATTACKS_ENABLED() && (this->timer == 20) && (this->colliderJntSph.base.acFlags & AC_ON)) {
+            // Boss Overhaul: the slam sends out a second shockwave
+            Audio_PlayActorSound2(&this->actor, NA_SE_EN_SHADEST_TAIKO_LOW);
+            BossSst_SpawnShockwave(this);
+            this->colliderCyl.base.atFlags |= AT_ON;
+            Collider_UpdateCylinder(&this->actor, &this->colliderCyl);
+            this->colliderCyl.dim.radius = sCylinderInitHand.dim.radius;
+        }
+
         if (this->timer == 0) {
             if (this->colliderJntSph.base.acFlags & AC_ON) {
                 BossSst_HandSetupEndSlam(this);
@@ -1575,7 +1598,7 @@ void BossSst_HandSetupSweep(BossSst* this) {
     Animation_MorphToPlayOnce(&this->skelAnime, sHandFlatPoses[this->actor.params], 5.0f);
     BossSst_HandSetDamage(this, 0x10);
     this->targetYaw = this->actor.home.rot.y - (this->vParity * 0x2000);
-    this->handMaxSpeed = 0x300;
+    this->handMaxSpeed = BO_ATTACKS_ENABLED() ? 0x3C0 : 0x300; // Boss Overhaul: faster sweep
     this->handAngSpeed = 0;
     this->ready = false;
     Audio_PlayActorSound2(&this->actor, NA_SE_EN_SHADEST_FLY_ATTACK);
@@ -1709,7 +1732,7 @@ void BossSst_HandReadyClap(BossSst* this, PlayState* play) {
 void BossSst_HandSetupClap(BossSst* this) {
     Animation_MorphToPlayOnce(&this->skelAnime, sHandFlatPoses[this->actor.params], 3.0f);
     this->timer = 0;
-    this->handMaxSpeed = 0x240;
+    this->handMaxSpeed = BO_ATTACKS_ENABLED() ? 0x2C0 : 0x240; // Boss Overhaul: faster clap
     this->handAngSpeed = 0;
     this->ready = false;
     BossSst_HandSetDamage(this, 0x20);
@@ -2488,6 +2511,11 @@ void BossSst_HandSelectAttack(BossSst* this) {
     f32 rand = Rand_ZeroOne() * 6.0f;
     s32 randInt;
 
+    if (BONGO_ENRAGED() && (HAND_STATE(OTHER_HAND(this)) != HAND_DAMAGED)) {
+        // Boss Overhaul: an enraged Bongo Bongo claps more often (claps use randInt 5 and up)
+        rand = Rand_ZeroOne() * 7.0f;
+    }
+
     if (HAND_STATE(OTHER_HAND(this)) == HAND_DAMAGED) {
         rand *= 5.0f / 6;
         if (rand > 4.0f) {
@@ -2502,7 +2530,7 @@ void BossSst_HandSelectAttack(BossSst* this) {
         BossSst_HandSetupReadySweep(this);
     } else if (randInt == 2) {
         BossSst_HandSetupReadyPunch(this);
-    } else if (randInt == 5) {
+    } else if (randInt >= 5) {
         BossSst_HandSetupReadyClap(this);
     } else { // randInt == 3 || randInt == 4
         BossSst_HandSetupReadyGrab(this);
diff --git a/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c b/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
index 3f83322..cb32ca8 100644
--- a/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
+++ b/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.c
@@ -8,6 +8,7 @@
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
 
 #include <string.h>
+#include "boss_overhaul.h"
 
 #define FLAGS                                                                                 \
     (ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_HOSTILE | ACTOR_FLAG_UPDATE_CULLING_DISABLED | \
@@ -25,6 +26,10 @@ typedef enum {
     /* 0x69 */ TW_DEATHBALL_KOUME
 } TwinrovaType;
 
+// Boss Overhaul: Twinrova is enraged when at or below half health (and extra attacks are enabled).
+#define TW_ENRAGED() \
+    (BO_ATTACKS_ENABLED() && (sTwinrovaPtr != NULL) && BO_BELOW_HALF((s8)sTwinrovaPtr->actor.colChkInfo.health, 24))
+
 void BossTw_Init(Actor* thisx, PlayState* play);
 void BossTw_Destroy(Actor* thisx, PlayState* play);
 void BossTw_Update(Actor* thisx, PlayState* play);
@@ -515,7 +520,7 @@ void BossTw_Init(Actor* thisx, PlayState* play2) {
         // Twinrova
         Collider_SetCylinder(play, &this->collider, &this->actor, &sCylinderInitTwinrova);
         this->actor.naviEnemyId = 0x5B;
-        this->actor.colChkInfo.health = 24;
+        this->actor.colChkInfo.health = BO_HP(24);
         this->actor.update = BossTw_TwinrovaUpdate;
         this->actor.draw = BossTw_TwinrovaDraw;
         SkelAnime_InitFlex(play, &this->skelAnime, &gTwinrovaSkel, &gTwinrovaTPoseAnim, NULL, NULL, 0);
@@ -643,6 +648,11 @@ void BossTw_SetupFlyTo(BossTw* this, PlayState* play) {
         this->timers[0] = 200;
         this->work[CAN_SHOOT] = true;
     }
+
+    if (BO_ATTACKS_ENABLED() && (Rand_ZeroOne() < 0.5f)) {
+        // Boss Overhaul: the sisters fire their beams much more often
+        this->work[CAN_SHOOT] = true;
+    }
 }
 
 void BossTw_FlyTo(BossTw* this, PlayState* play) {
@@ -670,7 +680,7 @@ void BossTw_FlyTo(BossTw* this, PlayState* play) {
     Math_ApproachS(&this->actor.shape.rot.y, yawTarget, 0xA, this->rotateSpeed);
     Math_ApproachS(&this->actor.shape.rot.x, pitchTarget, 0xA, this->rotateSpeed);
     Math_ApproachF(&this->rotateSpeed, 4096.0f, 1.0f, 100.0f);
-    Math_ApproachF(&this->actor.speedXZ, 10.0f, 1.0f, 1.0f);
+    Math_ApproachF(&this->actor.speedXZ, BO_ATTACKS_ENABLED() ? 13.0f : 10.0f, 1.0f, 1.0f);
     Actor_UpdateVelocityXYZ(&this->actor);
     Actor_UpdatePos(&this->actor);
 
@@ -1454,7 +1464,7 @@ void BossTw_SetupWait(BossTw* this, PlayState* play) {
 void BossTw_Wait(BossTw* this, PlayState* play) {
     if ((this->actor.params == TW_TWINROVA) && (sKoumePtr->actionFunc == BossTw_FlyTo) &&
         (sKotakePtr->actionFunc == BossTw_FlyTo) &&
-        ((sKoumePtr->actor.colChkInfo.health + sKotakePtr->actor.colChkInfo.health) >= 4)) {
+        ((sKoumePtr->actor.colChkInfo.health + sKotakePtr->actor.colChkInfo.health) >= BO_HP(4))) {
 
         BossTw_TwinrovaSetupMergeCS(this, play);
         BossTw_SetupMergeCS(sKotakePtr, play);
@@ -3106,7 +3116,7 @@ void BossTw_TwinrovaUpdate(Actor* thisx, PlayState* play2) {
             }
 
             if (!(info->toucher.dmgFlags & DMG_HOOKSHOT)) {
-                if (((s8)this->actor.colChkInfo.health < 3) && !swordDamage) {
+                if (((s8)this->actor.colChkInfo.health <= BO_HP(2)) && !swordDamage) {
                     damage = 0;
                 }
 
@@ -3905,7 +3915,8 @@ void BossTw_BlastFire(BossTw* this, PlayState* play) {
                     // pitch
                     distXZ = sqrtf(SQ(xDiff) + SQ(zDiff));
                     this->actor.world.rot.x = Math_FAtan2F(yDiff, distXZ) * (32768 / M_PI);
-                    this->actor.speedXZ = 20.0f;
+                    // Boss Overhaul: faster blasts
+                    this->actor.speedXZ = BO_ATTACKS_ENABLED() ? (TW_ENRAGED() ? 30.0f : 26.0f) : 20.0f;
 
                     for (i = 0; i < 50; i++) {
                         this->blastTailPos[i] = this->actor.world.pos;
@@ -4093,7 +4104,8 @@ void BossTw_BlastIce(BossTw* this, PlayState* play) {
                     this->actor.world.rot.y = Math_FAtan2F(xDiff, zDiff) * (32768 / M_PI);
                     xzDist = sqrtf(SQ(xDiff) + SQ(zDiff));
                     this->actor.world.rot.x = Math_FAtan2F(yDiff, xzDist) * (32768 / M_PI);
-                    this->actor.speedXZ = 20.0f;
+                    // Boss Overhaul: faster blasts
+                    this->actor.speedXZ = BO_ATTACKS_ENABLED() ? (TW_ENRAGED() ? 30.0f : 26.0f) : 20.0f;
                     for (i = 0; i < 50; i++) {
                         this->blastTailPos[i] = this->actor.world.pos;
                     }
@@ -5151,7 +5163,7 @@ void BossTw_TwinrovaChargeBlast(BossTw* this, PlayState* play) {
     Math_ApproachS(&this->actor.shape.rot.y, this->actor.yawTowardsPlayer, 5, 0x1000);
 
     if (Animation_OnFrame(&this->skelAnime, this->workf[ANIM_SW_TGT])) {
-        if ((s8)this->actor.colChkInfo.health < 10) {
+        if ((s8)this->actor.colChkInfo.health < BO_HP(10)) {
             sTwinrovaBlastType = Rand_ZeroFloat(1.99f);
         } else {
             if (++sFixedBlatSeq >= 4) {
@@ -5233,14 +5245,18 @@ void BossTw_TwinrovaShootBlast(BossTw* this, PlayState* play) {
 void BossTw_TwinrovaSetupDoneBlastShoot(BossTw* this, PlayState* play) {
     this->actionFunc = BossTw_TwinrovaDoneBlastShoot;
     Animation_MorphToLoop(&this->skelAnime, &gTwinrovaHoverAnim, -10.0f);
-    this->timers[1] = 60;
+    this->timers[1] = BO_ATTACKS_ENABLED() ? 40 : 60;
 }
 
 void BossTw_TwinrovaDoneBlastShoot(BossTw* this, PlayState* play) {
     SkelAnime_Update(&this->skelAnime);
 
     if (this->timers[1] == 0 && D_8094C870 == 0) {
-        if (sTwinrovaPtr->timers[2] == 0) {
+        if ((sTwinrovaPtr->timers[2] == 0) && TW_ENRAGED() && (this->boBlastCombo == 0)) {
+            // Boss Overhaul: an enraged Twinrova immediately follows up with a second blast before flying away
+            this->boBlastCombo = 1;
+            BossTw_TwinrovaSetupChargeBlast(this, play);
+        } else if (sTwinrovaPtr->timers[2] == 0) {
             BossTw_TwinrovaSetupFly(this, play);
         } else {
             BossTw_TwinrovaSetupLaugh(this, play);
@@ -5384,6 +5400,7 @@ void BossTw_TwinrovaSetupFly(BossTw* this, PlayState* play) {
     yDiff = this->targetPos.y - this->actor.world.pos.y;
     zDiff = this->targetPos.z - this->actor.world.pos.z;
     this->actionFunc = BossTw_TwinrovaFly;
+    this->boBlastCombo = 0;
     this->rotateSpeed = 0.0f;
     this->actor.speedXZ = 0.0f;
     this->actor.world.rot.y = Math_FAtan2F(xDiff, zDiff) * (32768 / M_PI);
@@ -5414,7 +5431,7 @@ void BossTw_TwinrovaFly(BossTw* this, PlayState* play) {
     Math_ApproachS(&this->actor.world.rot.y, yaw, 0xA, this->rotateSpeed);
     Math_ApproachS(&this->actor.shape.rot.y, yaw, 0xA, this->rotateSpeed);
     Math_ApproachF(&this->rotateSpeed, 2000.0f, 1.0f, 100.0f);
-    Math_ApproachF(&this->actor.speedXZ, 30.0f, 1.0f, 2.0f);
+    Math_ApproachF(&this->actor.speedXZ, BO_ATTACKS_ENABLED() ? 36.0f : 30.0f, 1.0f, 2.0f);
     Actor_UpdateVelocityXYZ(&this->actor);
     Math_ApproachF(&this->actor.world.pos.x, this->targetPos.x, 0.1f, fabsf(this->actor.velocity.x) * 1.5f);
     Math_ApproachF(&this->actor.world.pos.y, this->targetPos.y, 0.1f, fabsf(this->actor.velocity.y) * 1.5f);
diff --git a/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.h b/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.h
index c49d456..ca6297f 100644
--- a/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.h
+++ b/soh/src/overlays/actors/ovl_Boss_Tw/z_boss_tw.h
@@ -175,6 +175,7 @@ typedef struct BossTw {
     /* 0x06A8 */ char unused_6A8[4];
     /* 0x06AC */ f32 subCamYaw;
     /* 0x06B0 */ f32 subCamYawStep;
-} BossTw; // size = 0x06B4
+    s16 boBlastCombo; // Boss Overhaul: Twinrova's chained blast counter
+} BossTw; // size = 0x06B4 (vanilla)
 
 #endif
diff --git a/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c b/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
index f30f966..68cea66 100644
--- a/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
+++ b/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.c
@@ -19,6 +19,8 @@
 #include "soh/Enhancements/game-interactor/GameInteractor.h"
 #include "soh/Enhancements/game-interactor/GameInteractor_Hooks.h"
 
+#include "boss_overhaul.h"
+
 #define FLAGS                                                                                 \
     (ACTOR_FLAG_ATTENTION_ENABLED | ACTOR_FLAG_HOSTILE | ACTOR_FLAG_UPDATE_CULLING_DISABLED | \
      ACTOR_FLAG_DRAW_CULLING_DISABLED)
@@ -587,6 +589,34 @@ void BossVa_KillBari(BossVa* this, PlayState* play) {
     BossVa_SetupBariDeath(this);
 }
 
+// Boss Overhaul (Double HP): every Bari survives its first hit. Returns true if the Bari was killed.
+static s32 BossVa_DamageBari(BossVa* this, PlayState* play) {
+    if (BO_HP_ENABLED()) {
+        if (this->hitsTaken == 0) {
+            this->hitsTaken = 1;
+            this->hitFrame = play->gameplayFrames;
+            this->colliderSph.base.acFlags &= ~AC_HIT;
+            Actor_SetColorFilter(&this->actor, 0x4000, 255, 0, 12);
+            Audio_PlayActorSound2(&this->actor, NA_SE_EN_BALINADE_DAMAGE);
+            return false;
+        }
+
+        if ((play->gameplayFrames - this->hitFrame) < 10) {
+            // still the same sword swing
+            this->colliderSph.base.acFlags &= ~AC_HIT;
+            return false;
+        }
+    }
+
+    BossVa_KillBari(this, play);
+    return true;
+}
+
+// Boss Overhaul (Extra Attacks): Barinade's core moves faster.
+static f32 BossVa_CoreSpeedScale(void) {
+    return BO_ATTACKS_ENABLED() ? 1.35f : 1.0f;
+}
+
 void BossVa_Init(Actor* thisx, PlayState* play2) {
     PlayState* play = play2;
     BossVa* this = (BossVa*)thisx;
@@ -1362,7 +1392,7 @@ void BossVa_SetupBodyPhase4(BossVa* this, PlayState* play) {
     this->actor.world.rot.y = this->actor.yawTowardsPlayer;
     this->timer2 = (s16)(Rand_ZeroOne() * 150.0f) + 300;
     sBodyState = 1;
-    sPhase4HP = 4;
+    sPhase4HP = BO_HP(4);
     if (this->actor.shape.yOffset != 0.0f) {
         this->timer = -30;
     }
@@ -1373,6 +1403,7 @@ void BossVa_SetupBodyPhase4(BossVa* this, PlayState* play) {
 
 void BossVa_BodyPhase4(BossVa* this, PlayState* play) {
     Player* player = GET_PLAYER(play);
+    s32 pulsing = false;
     f32 tmpf1;
     EnBoom* boomerang;
 
@@ -1404,7 +1435,7 @@ void BossVa_BodyPhase4(BossVa* this, PlayState* play) {
                     if (sPhase4HP <= 0) {
                         this->timer = 0;
                         sFightPhase++;
-                        sPhase4HP += 3;
+                        sPhase4HP += BO_HP(3);
                         if (sFightPhase >= PHASE_DEATH) {
                             BossVa_SetupBodyDeath(this, play);
                             Enemy_StartFinishingBlow(play, &this->actor);
@@ -1445,7 +1476,8 @@ void BossVa_BodyPhase4(BossVa* this, PlayState* play) {
                 this->actor.world.rot.y = this->actor.yawTowardsPlayer;
                 this->timer2 = (s16)(Rand_ZeroOne() * 150.0f) + 300;
             }
-            Math_SmoothStepToF(&this->actor.speedXZ, ((sFightPhase - PHASE_4 + 1) * 1.5f) + 4.0f, 1.0f, 0.25f, 0.0f);
+            Math_SmoothStepToF(&this->actor.speedXZ, (((sFightPhase - PHASE_4 + 1) * 1.5f) + 4.0f) * BossVa_CoreSpeedScale(),
+                               1.0f, 0.25f, 0.0f);
         }
         this->colliderBody.info.bumper.dmgFlags = 0x10;
     } else {
@@ -1476,7 +1508,8 @@ void BossVa_BodyPhase4(BossVa* this, PlayState* play) {
                 }
                 Math_SmoothStepToS(&this->vaBodySpinRate, 0xFA0, 1, 0x1F4, 0);
                 tmpf1 = sFightPhase - PHASE_4 + 1;
-                Math_SmoothStepToF(&this->actor.speedXZ, (tmpf1 + tmpf1) + 4.0f, 1.0f, 0.25f, 0.0f);
+                Math_SmoothStepToF(&this->actor.speedXZ, ((tmpf1 + tmpf1) + 4.0f) * BossVa_CoreSpeedScale(), 1.0f, 0.25f,
+                                   0.0f);
                 Math_SmoothStepToF(&this->actor.shape.yOffset, 0.0f, 1.0f, 20.0f, 0.0f);
             }
             this->timer++;
@@ -1516,13 +1549,30 @@ void BossVa_BodyPhase4(BossVa* this, PlayState* play) {
         BossVa_Spark(play, this, 1, 100, 15.0f, 10.0f, SPARK_BARI, 11.0f, true);
     }
 
+    if (BO_ATTACKS_ENABLED()) {
+        // Boss Overhaul: periodic overcharge pulse. The core's electric aura expands and shocks everything near it.
+        u32 pulseCycle = play->gameplayFrames % ((sFightPhase >= PHASE_4 + 2) ? 60 : 90);
+
+        if (this->timer == 0 && this->actor.shape.yOffset > -30.0f && pulseCycle < 16) {
+            if (pulseCycle == 0) {
+                Audio_PlayActorSound2(&this->actor, NA_SE_EN_BALINADE_THUNDER);
+                BossVa_SetSparkEnv(play);
+            }
+            this->colliderBody.dim.radius = 140;
+            BossVa_Spark(play, this, 3, 110, 90.0f, 30.0f, SPARK_BLAST, 8.0f, true);
+            pulsing = true;
+        } else {
+            this->colliderBody.dim.radius = 55;
+        }
+    }
+
     Actor_UpdateBgCheckInfo(play, &this->actor, 30.0f, 70.0f, 0.0f, 1);
     Collider_UpdateCylinder(&this->actor, &this->colliderBody);
     CollisionCheck_SetOC(play, &play->colChkCtx, &this->colliderBody.base);
     if (this->invincibilityTimer == 0) {
         CollisionCheck_SetAC(play, &play->colChkCtx, &this->colliderBody.base);
     }
-    if ((this->vaBodySpinRate > 0x3E8) || (this->actor.shape.yOffset < -1200.0f)) {
+    if ((this->vaBodySpinRate > 0x3E8) || (this->actor.shape.yOffset < -1200.0f) || pulsing) {
         CollisionCheck_SetAT(play, &play->colChkCtx, &this->colliderBody.base);
     }
     func_800F436C(&this->actor.projectedPos, NA_SE_EN_BALINADE_LEVEL - SFX_FLAG,
@@ -1766,6 +1816,14 @@ void BossVa_SupportAttached(BossVa* this, PlayState* play) {
         Audio_PlayActorSound2(&this->actor, NA_SE_EN_BALINADE_BL_SPARK - SFX_FLAG);
     }
 
+    if ((this->colliderSph.base.acFlags & AC_HIT) && BO_HP_ENABLED() && this->hitsTaken == 0) {
+        // Boss Overhaul (Double HP): the tendril has to be hit twice before it is cut
+        this->hitsTaken = 1;
+        this->colliderSph.base.acFlags &= ~AC_HIT;
+        Actor_SetColorFilter(&this->actor, 0x4000, 255, 0, 12);
+        Audio_PlayActorSound2(&this->actor, NA_SE_EN_BALINADE_DAMAGE);
+    }
+
     if (this->colliderSph.base.acFlags & AC_HIT) {
         BossVa_SetupSupportCut(this, play);
     } else {
@@ -2126,6 +2184,10 @@ void BossVa_ZapperAttack(BossVa* this, PlayState* play) {
             }
         }
 
+        if (BO_ATTACKS_ENABLED() && sFightPhase >= PHASE_3 && this->timer2 < 30 && (this->timer2 & 1) == 0) {
+            // Boss Overhaul: once the tendrils are gone the zappers charge their lightning twice as fast
+            this->timer2++;
+        }
         this->timer2++;
         if (this->timer2 >= 40) {
             this->burst = false;
@@ -2658,8 +2720,9 @@ void BossVa_BariPhase2Attack(BossVa* this, PlayState* play) {
     this->vaBariUnused.y += this->vaBariUnused.z;
     if (sKillBari != 0) {
         sKillBari--;
-        BossVa_KillBari(this, play);
-        return;
+        if (BossVa_DamageBari(this, play)) {
+            return;
+        }
     }
 
     if ((this->colliderLightning.base.atFlags & AT_HIT) || (this->colliderSph.base.atFlags & AT_HIT)) {
@@ -2720,7 +2783,7 @@ void BossVa_BariPhase2Attack(BossVa* this, PlayState* play) {
 
         this->actor.world.pos.y += Math_SinF(this->unk_1A4) * 4.0f;
         if (this->colliderSph.base.acFlags & AC_HIT) {
-            BossVa_KillBari(this, play);
+            BossVa_DamageBari(this, play);
         }
     }
 
@@ -2759,8 +2822,9 @@ void BossVa_BariPhase3Stunned(BossVa* this, PlayState* play) {
 
     this->actor.world.rot.x = Math_Vec3f_Pitch(&GET_BODY(this)->actor.world.pos, &this->actor.world.pos);
     if (this->colliderSph.base.acFlags & AC_HIT) {
-        BossVa_KillBari(this, play);
-        return;
+        if (BossVa_DamageBari(this, play)) {
+            return;
+        }
     }
 
     this->unk_1A4 += Rand_ZeroOne() * 0.5f;
diff --git a/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.h b/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.h
index cb00261..c49903b 100644
--- a/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.h
+++ b/soh/src/overlays/actors/ovl_Boss_Va/z_boss_va.h
@@ -45,6 +45,8 @@ typedef struct BossVa {
     /* 0x02D8 */ ColliderJntSph colliderSph;
     /* 0x02F8 */ ColliderJntSphElement elements[1];
     /* 0x0338 */ ColliderQuad colliderLightning;
+    u8 hitsTaken; // Boss Overhaul: hits absorbed by a tendril/Bari before it is cut/killed
+    u32 hitFrame; // Boss Overhaul: gameplay frame of the last absorbed hit
 } BossVa; // size = 0x03B8
 
 typedef enum {
diff --git a/soh/src/overlays/actors/ovl_En_Goma/z_en_goma.c b/soh/src/overlays/actors/ovl_En_Goma/z_en_goma.c
index fa95087..d2539ac 100644
--- a/soh/src/overlays/actors/ovl_En_Goma/z_en_goma.c
+++ b/soh/src/overlays/actors/ovl_En_Goma/z_en_goma.c
@@ -147,7 +147,7 @@ void EnGoma_Init(Actor* thisx, PlayState* play) {
         Animation_PlayLoop(&this->skelanime, &gObjectGolStandAnim);
         this->actor.colChkInfo.health = 2;
 
-        if (this->actor.params < 3) { // Spawned by boss
+        if (this->actor.params < 6) { // Spawned by boss (params 3-5 are the extra children of Boss Overhaul)
             this->actionFunc = EnGoma_EggFallToGround;
             this->invincibilityTimer = 10;
             this->actor.speedXZ = 1.5f;
diff --git a/soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c b/soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c
index 3c83a1d..ae65acb 100644
--- a/soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c
+++ b/soh/src/overlays/actors/ovl_En_fHG/z_en_fhg.c
@@ -11,6 +11,8 @@
 #include "overlays/actors/ovl_Boss_Ganondrof/z_boss_ganondrof.h"
 #include "overlays/actors/ovl_En_Fhg_Fire/z_en_fhg_fire.h"
 
+#include "boss_overhaul.h"
+
 #define FLAGS ACTOR_FLAG_UPDATE_CULLING_DISABLED
 
 typedef struct {
@@ -617,7 +619,7 @@ void EnfHG_Damage(EnfHG* this, PlayState* play) {
         this->timers[0] = 140;
         this->actionFunc = EnfHG_Retreat;
         Animation_MorphToLoop(&this->skin.skelAnime, &gPhantomHorseRunningAnim, 0.0f);
-        if (bossGnd->actor.colChkInfo.health > 24) {
+        if (bossGnd->actor.colChkInfo.health > BO_HP(24)) {
             this->bossGndSignal = FHG_RIDE;
         } else {
             bossGnd->flyMode = GND_FLY_NEUTRAL;
